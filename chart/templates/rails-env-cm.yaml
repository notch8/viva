---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $fullName }}
data:
  DATABASE_HOST: {{ template "app.postgres.name" . }}
{{- range $key, $value := .Values.env.configmap }}
  {{ $key }}: {{ $value | quote }}
{{- end }}